# Migration `20191126101851`

This migration has been generated by Nikolas Burk at 11/26/2019, 10:18:51 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Post" ADD COLUMN "views" integer NOT NULL DEFAULT 0 ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191126100446..20191126101851
--- datamodel.dml
+++ datamodel.dml
@@ -3,17 +3,17 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://nikolasburk:nikolasburk@localhost:5432/mydb3"
 }
 model User {
-  id    String  @default(cuid()) @id
-  email String  @unique
-  firstName  String
-  lastName    String
-  posts Post[]
+  id        String @default(cuid()) @id
+  email     String @unique
+  firstName String
+  lastName  String
+  posts     Post[]
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -21,6 +21,7 @@
   updatedAt DateTime @updatedAt
   published Boolean  @default(true)
   title     String
   content   String?
+  views     Int      @default(0)
   author    User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191126101851)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191126101851'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
